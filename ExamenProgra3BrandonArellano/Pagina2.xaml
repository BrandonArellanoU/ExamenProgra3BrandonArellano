<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             Title="Historial de Países"
             x:Class="ExamenProgra3BrandonArellano.Views.HistorialPage">
    <VerticalStackLayout Padding="20">
        <ListView x:Name="ListaPaises">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Display}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>
